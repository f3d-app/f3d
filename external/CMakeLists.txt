if (F3D_MODULE_CLIP AND NOT F3D_USE_EXTERNAL_CLIP)
  set(CLIP_ENABLE_IMAGE ON)
  set(CLIP_ENABLE_LIST_FORMATS OFF)
  set(CLIP_EXAMPLES OFF)
  set(CLIP_TESTS OFF)
  set(CLIP_INSTALL ON)
  set(CLIP_X11_WITH_PNG ON)
  set(CLIP_SUPPORT_WINXP OFF)
  add_subdirectory(clip)
endif()  
